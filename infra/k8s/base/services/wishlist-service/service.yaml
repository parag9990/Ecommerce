apiVersion: v1
kind: Service
metadata:
  name: wishlist-service
  namespace: core
  labels:
    app.kubernetes.io/name: wishlist-service
    app.kubernetes.io/part-of: ecommerce-platform
spec:
  type: ClusterIP
  selector: { app.kubernetes.io/name: wishlist-service }
  ports:
    - { name: http, port: 8084, targetPort: http, protocol: TCP }
