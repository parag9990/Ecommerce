apiVersion: v1
kind: Service
metadata:
  name: superadmin-service
  namespace: core
  labels:
    app.kubernetes.io/name: superadmin-service
    app.kubernetes.io/part-of: ecommerce-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: superadmin-service
  ports:
    - { name: http, port: 8088, targetPort: http, protocol: TCP }
