apiVersion: v1
kind: Service
metadata:
  name: cart-service
  namespace: core
  labels:
    app.kubernetes.io/name: cart-service
    app.kubernetes.io/part-of: ecommerce-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cart-service
  ports:
    - { name: http, port: 8083, targetPort: http, protocol: TCP }
