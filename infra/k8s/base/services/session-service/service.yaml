apiVersion: v1
kind: Service
metadata:
  name: session-service
  namespace: core
  labels:
    app.kubernetes.io/name: session-service
    app.kubernetes.io/part-of: ecommerce-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: session-service
  ports:
    - { name: http, port: 8086, targetPort: http, protocol: TCP }
