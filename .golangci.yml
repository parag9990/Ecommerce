version: "2"
run:
  timeout: 5m
linters:
  default: standard
  enable:
    - bodyclose
    - errorlint
    - govet
    - ineffassign
    - misspell
    - staticcheck
formatters:
  enable:
    - gofmt
    - goimports
